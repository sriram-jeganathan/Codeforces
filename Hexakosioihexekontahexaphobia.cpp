//470B
#include <iostream>
#include <string>
using namespace std;

int main ( void ) {
    string str;
    cin >> str;
    int len = str.length();
    int count = 0;
    for ( int i = 0; i < len - 1; i++ ) {
        if ( ( len[i] == len[i+1] ) && ( len) )
    }
}
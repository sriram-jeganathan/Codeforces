//230B
#include <iostream>
#include <cmath>
using namespace std;

int main ( void ) {
    int n;
    long long a,b,c;
    cin >> n;
    long long roota = sqrt(a), rootb = sqrt(b), rootc = sqrt(c);

    vector<long long> num = {a,b,c};
}



    